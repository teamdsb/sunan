# 财务板块流程图（M6 Wave C 自生成补料基线）

## 1. 说明
- 生成日期：2026-04-22
- 适用模块：`finance_business_board`
- 范围：提单 -> 审批 -> 统计 -> 导出 -> 打印归档

## 2. 流程图
```mermaid
flowchart TD
  A["财务人员新建财务台账"] --> B["保存草稿"]
  B --> C{"是否提交审批"}
  C -- "否" --> B
  C -- "是" --> D["提交审批（企业微信/系统内）"]
  D --> E{"审批结果"}
  E -- "驳回" --> F["退回修改并留痕"]
  F --> B
  E -- "通过" --> G["状态改为已通过"]
  G --> H["进入月度统计口径"]
  H --> I["生成导出任务（xlsx/pdf）"]
  I --> J["打印快照（A4）"]
  J --> K["归档与审计追踪"]
```

## 3. 角色职责
- 财务录单人：创建、编辑、提交。
- 财务审核人：审批通过/驳回。
- 管理员：统计导出、打印、归档审计。


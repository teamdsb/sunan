---
status: historical-archive
owner: guides
updated: 2026-05-04
replaces: []
replaced_by: []
---
# "我的"模块 QA 测试指南

## 适用范围

本文档记录 M1 "我的" 模块早期 QA 手动测试场景。旧运行时 mock 和假用户认证已经停用，执行场景时必须连接真实后端并使用真实认证。

适用对象：

- QA 测试工程师
- 使用 AI 工具辅助测试的研发人员
- 需要在本地复现问题的产品或开发同学

## 环境搭建

### 前提条件

| 组件 | 建议版本 |
|---|---|
| Node.js | 20 LTS |
| pnpm | 9+ |
| Docker Desktop | 最新稳定版 |

### 快速启动

1. 安装依赖：`pnpm install`
2. 启动数据库：`make db-up`
3. 执行 migration：`make migration-run`
4. 初始化种子：`make seed`
5. 启动服务：`make start-api` 与 `make dev`

默认服务地址：

- API: `http://127.0.0.1:3000`
- Web: `http://127.0.0.1:5173`

### 真实联调说明

- 前端不支持跳过企业微信认证，也不会注入本地预览用户。
- OAuth2、JS-SDK、组织通讯录映射必须在真实企业微信环境验证。
- 不同角色场景应使用对应真实测试账号和后端种子数据覆盖。

### 数据库重置

当数据被污染或需要重新回到初始状态时，执行：

```bash
make db-reset
make migration-run
make seed
```

## 自动化测试覆盖说明

### 已覆盖，无需手动重复

- 单元测试：规则函数、状态计算、格式化逻辑
- 集成测试：主要 API 与数据库真实行为
- 组件测试：核心页面交互、加载和提交行为

### 仍需手动测试的领域

- 视觉回归与版面稳定性
- 企业微信 JS-SDK 相关行为
- 真实设备兼容性
- 弱网与异常网络表现
- 打印与导出体验

## 子功能测试场景

### 1. 企业资料

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 列表加载 | 进入企业资料页面 | 列表成功展示，分页信息正确 | P0 |
| 2 | 详情查看 | 点击任一企业资料项 | 进入详情页，字段与列表项一致 | P0 |
| 3 | 附件查看 | 在详情页查看已绑定附件 | 附件可见，可正确打开或下载 | P1 |
| 4 | 权限控制 | 使用无管理权限用户访问编辑动作 | 仅展示允许的操作，不出现越权入口 | P0 |

### 2. 企业制度

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 列表加载 | 进入企业制度页面 | 列表与分页正常展示 | P0 |
| 2 | 详情查看 | 打开任一制度详情 | 正文、发布时间、状态显示正确 | P0 |
| 3 | 版本历史 | 查看存在多版本的制度 | 历史版本顺序正确，可区分当前版本 | P1 |
| 4 | 上传发布 | 以有权限账号上传新制度 | 上传成功，列表与详情同步更新 | P0 |
| 5 | 下载文件 | 下载制度附件 | 文件可下载，名称与内容正确 | P1 |

### 3. 电子证照

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 分组查看 | 切换不同分组条件 | 列表按分组规则正确变化 | P0 |
| 2 | 新增证照 | 创建新证照并填写必要字段 | 保存成功，列表可见 | P0 |
| 3 | 编辑证照 | 修改已有证照信息 | 更新后详情与列表同步变化 | P0 |
| 4 | 删除证照 | 删除可删除证照 | 前台不可见，软删除数据不再出现在默认列表 | P0 |
| 5 | 到期状态 | 查看临期、已过期证照 | 到期状态计算准确 | P0 |
| 6 | 搜索筛选 | 输入关键词与筛选条件 | 结果匹配条件，分页重置合理 | P1 |

### 4. 证书提醒

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 看板查看 | 进入提醒看板 | 提醒统计与列表加载正常 | P0 |
| 2 | 提醒确认 | 选择一条提醒并确认 | 状态更新成功，重复确认被正确处理 | P0 |
| 3 | 手动扫描 | 触发手动扫描 | 新提醒按规则生成或返回空结果 | P0 |
| 4 | 部门过滤 | 按部门筛选提醒 | 仅显示当前条件下的数据 | P1 |

### 5. 船舶监控

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 列表查看 | 进入船舶监控页面 | 入口列表正常展示 | P0 |
| 2 | 按船查看 | 选择不同船舶 | 监控项按船切换正确 | P0 |
| 3 | 外链跳转 | 点击监控外链 | 在新窗口或新标签页打开正确地址 | P1 |

### 6. 设置

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 查看设置 | 打开设置页面 | 当前偏好成功回显 | P0 |
| 2 | 修改设置 | 修改任一设置项并保存 | 保存成功，反馈明确 | P0 |
| 3 | 持久化验证 | 刷新页面或重新进入设置页 | 修改后的值仍然存在 | P0 |

### 7. 办事治理（M2 Wave4）

| 序号 | 场景 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| 1 | 分类越权拦截 | 使用 `shipping` 角色尝试创建 `customs` 分类入口 | 返回 403，且无数据写入 | P0 |
| 2 | 状态可见性 | 创建 `draft` 入口并在普通用户端查询目录 | 普通用户不可见 | P0 |
| 3 | 发布可见性 | 将 `draft` 发布后在普通用户端查询目录 | 入口可见且可打开 | P0 |
| 4 | 停用可见性 | 将 `published` 停用后在普通用户端查询目录 | 入口不可见 | P0 |
| 5 | URL 校验 | 提交 `http://` 或裸 IP 外部地址 | 返回 400，提示地址不合法 | P0 |
| 6 | 路由白名单校验 | 提交不在白名单的站内路由 | 返回 400，提示路由不合法 | P0 |
| 7 | 审计记录查询 | 在治理台查看最新审计记录 | 可看到动作、入口、操作人、时间 | P0 |
| 8 | 打开行为审计 | 普通用户打开办事入口后查询审计 | 可看到 `open` 动作记录 | P0 |

## Wave4 联调验收步骤

1. 在企业微信 iOS 客户端执行一次完整链路：
`登录 -> /office -> 打开外部入口 -> /office/admin -> 发布/停用 -> 查询审计`。
2. 在企业微信 Android 客户端执行同样链路。
3. 导出或截图以下证据：
- `/office` 普通用户仅看到已发布入口
- `/office/admin` 审计表存在 `create/update/publish/disable/open`
- 外部地址与站内路由均可打开

## 通用测试检查项

每个子功能至少补充检查以下通用项：

- 分页：页码、页大小、总数、翻页后数据是否正确
- 空状态：无数据时文案、占位和操作提示是否合理
- 加载状态：骨架屏或 loading 是否明确，是否存在闪烁
- 错误状态：接口失败后是否给出可理解提示，是否支持重试
- 软删除不可见：被删除数据不应出现在默认查询结果中
- 时区：所有时间按 `+08:00` 口径展示与计算
- 响应格式：接口是否保持统一 API 信封格式

## 评估标准

| 维度 | 权重 | 关注点 |
|---|---|---|
| 功能完整性 | 40% | 6 个子功能可访问，CRUD 完整，权限正确 |
| 数据准确性 | 20% | 种子数据正确（11 船 1 车），到期计算准确，分页 meta 正确 |
| 用户体验 | 20% | 加载小于 2 秒，无布局错位，中文正确，移动端适配 |
| 稳定性 | 20% | 无崩溃，重复提交防护有效，并发操作无异常 |

建议评分方式：

- 90-100：可进入验收或预发布
- 75-89：存在少量中低风险问题，可排期修复后复测
- 60-74：存在明显缺陷，不建议继续流转
- 60 以下：核心功能或稳定性不达标，需要回归开发

## AI 工具辅助测试指南

### Claude Code 用法

适合以下辅助工作：

- 根据 spec 生成手动测试用例草稿
- 对照 OpenAPI 检查接口契约是否一致
- 基于现有测试文件分析覆盖盲区

示例提示词：

```text
请根据 docs/specs/my/ 下的规格，为“电子证照”模块生成一组 QA 手动测试用例。
要求覆盖 CRUD、搜索筛选、到期状态、附件绑定、异常场景，并按 P0/P1/P2 标注优先级。
```

```text
请对照 docs/specs/my/api/certificate-api.yaml 与当前前端调用行为，
列出参数、响应结构、状态码上的不一致点，并给出需要补测的场景。
```

### Prism Mock Server 用法

当后端接口尚未全部可用时，可基于 API 规格独立验证前端页面行为：

```bash
npx @stoplight/prism-cli mock docs/specs/my/api/certificate-api.yaml
```

适用场景：

- 前端独立联调列表、详情、空状态、错误状态
- QA 提前熟悉页面路径与基础交互
- 校验前端是否依赖未约定字段

### 覆盖盲区分析建议

让 AI 工具重点关注以下问题：

- 是否遗漏权限差异场景
- 是否遗漏弱网、超时、重复提交场景
- 是否遗漏文件上传和下载失败场景
- 是否遗漏移动端布局与长文本换行问题

## 输出建议

QA 提交测试结果时，建议至少包含：

- 测试日期与环境
- 使用账号与角色
- 执行范围
- 缺陷列表与严重级别
- 最终评分与是否建议通过

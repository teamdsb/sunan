# ADR-001 企业微信 H5 单页应用

## 状态

`accepted`

## 背景

系统必须运行在企业微信工作台内，且需要复用企业微信登录、JS-SDK 和消息推送能力。

## 决策

采用企业微信 H5 自建应用 + React 单页应用模式，不做小程序或原生 App 双端实现。

## 影响

- 优点：接入企业微信能力直接，部署更新成本低。
- 代价：需处理企业微信 WebView、iOS URL 签名、缓存等兼容问题。
- 后续：所有前端路由、鉴权和 JS-SDK 初始化需围绕 SPA 场景设计。

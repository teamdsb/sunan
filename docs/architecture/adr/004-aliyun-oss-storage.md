# ADR-004 阿里云 OSS 文件存储

## 状态

`accepted`

## 背景

系统涉及证照、制度、企业资料等大量附件，不适合后端中转和数据库二进制存储。

## 决策

文件二进制存 OSS，数据库只保存 `files` 元数据，前端通过预签名 URL 直传和下载。

## 影响

- 优点：减轻后端带宽压力，上传链路简单。
- 代价：需要额外处理上传回调、文件回收和访问控制。
- 后续：文件业务表统一通过关联表使用 `files`，不在主表直接存附件信息。

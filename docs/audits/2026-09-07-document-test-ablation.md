---
status: audit-snapshot
owner: repository
updated: 2026-09-07
replaces: []
replaced_by: []
---
# 文档与测试消融记录

## 最终处理

- AGENTS.md 仅保留项目简介、项目导航与常用命令、工作约定三个部分。按用户后续要求统一为中文，保留专业术语、路径和命令。
- CLAUDE.md 仅引用 AGENTS.md，移除重复导航、命令和强制先写测试的约束。
- 删除明确被取代的 M5 企业微信真机模板和 M6 财务 C-1 初判报告。保留当前替代说明并修复索引与历史引用；旧原文可从 Git 查回。
- 删除基于“仓库极早期”前提、与当前用户约定不符的 ADR-005；将冗长的 SDD 强制流程收敛为按原始目标和实际影响验证的短指南，同步测试策略。
- 删除 `apps/web/src/app/dashboardLayoutCss.test.ts`（19 条）、`pagePresentationPolicy.test.ts`（2 条）、`navigationUiPolicy.test.ts`（1 条）。三者读取源码字符串，锁定 CSS 具体值、JSX 排列或按钮文字，没有运行真实布局或导航。删除后不再自动约束这些具体表现，未声称其视觉检查已由其他测试替代。
- 保留认证、权限、业务状态、接口、文件、路由、数据持久化测试，以及包含独有发布/验收证据的历史材料。需求和领域规格的偏差登记到 [原始目标核查](2026-09-07-original-goal-audit.md)，未将存在偏差的文档一概删除。

## 消融实验

控制条件：业务源码、测试配置、依赖版本保持不变，同一 Node 20.20.2 / pnpm 10.33.0 环境，分别运行包含和不包含上述三个文件的完整前端测试集。依赖按锁文件补齐，测试前临时恢复原文件，基线完成后重新删除。

| 指标 | 删除前 | 删除后 |
|---|---|---|
| 通过文件 | 60 / 60 | 57 / 57 |
| 通过测试 | 270 / 270 | 248 / 248 |
| 失败 | 0 | 0 |
| 单次耗时 | 19.41 秒 | 15.51 秒 |

差额恰为 3 个文件、22 条源码断言，保留测试通过。单次耗时受并发工作和缓存影响，不据此声称性能提升。该实验验证清理没有造成现有测试回归，不能证明业务目标全部兑现，也不是 Agent 任务质量的统计实验。

首轮曾在依赖不完整的 Node 24 环境得到 15 个文件加载失败；本轮按用户授权补齐环境后已消除。首轮结果不再作为最终结论。

额外验证：后端单元 24 套件/125 条、PostgreSQL 集成 18 套件/84 条、前后端构建通过。原始目标核查另以临时测试复现两项流程缺陷，两项正确性断言失败；具体步骤和原因见原始目标核查报告，不能用现有测试集通过掩盖。

文档重新生成清单、检查链接与格式。AGENTS.md 采用普通 Markdown，仅免除该文件的 YAML 元数据检查，清单与链接检查保留。

## 原始材料对照

用户重新提供的 Downloads 压缩包已可读取，主要链路核查完成，结果见 [原始目标核查](2026-09-07-original-goal-audit.md)。原件、源码和测试揭示的缺口已逐项记录，业务修复与企业微信真机验证尚未执行。

## MVP 补充提交前的再次精简

用户明确希望减少单次改动需要同步的规格和测试文件。本次将工作平台运行时 DB、记录 State、模板 UI，以及证照 DB、提醒 DB、采购单 DB 共 6 份实现镜像原地封存为 `historical-archive`，保留旧正文和路径供追溯，撤去本次逐层补写的重复规则。当前实现以代码、entity、migration 为准，业务修复与迁移操作集中在原件核查报告；仅保留本次 2 份实际变更的 OpenAPI 契约。工作平台 README 改为简短的代码导航，开发流程不再要求每次同步四层规格。

删除 `MyHomePage.test.tsx` 中 6 项固定 CSS、旧图形、图标颜色和重复文案的断言（107 行）；保留 6 项入口、真实数据、提醒统计和加载失败行为测试。纯展示断言不封存在测试目录中，原文可从 Git 找回。未修改业务源码或放宽断言来配合删除。

本轮新增的自查 8 项 PostgreSQL 集成与 3 项组件测试继续保留；采购并发、换证回滚及提醒周期测试也保留。它们验证不同边界的真实失败，不是重复描述代码；其中照片时钟测试运行已经暴露并推动修复实际问题。后端测试隔离了企微/OSS，不能称为真实设备 E2E。

本轮精简前前端 58 文件 / 252 项通过；删减后首页剩余 6 项定向通过，最终全量 58 文件 / 246 项通过，差额恰为删除的 6 项。后端重新构建通过，不因文档和展示断言清理重复全量运行，沿用业务补充完成时的 125 项单元、96 项集成验证。企微真机验收仍待执行，代码提交不等于生产验收。

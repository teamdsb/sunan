import type { MigrationInterface, QueryRunner } from 'typeorm';
export class Wave3EvidenceExport1710000017000 implements MigrationInterface {
  name = 'Wave3EvidenceExport1710000017000';
  async up(q: QueryRunner) { await q.query(`CREATE TABLE evidence_records (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), business_type VARCHAR(64) NOT NULL, business_id UUID NOT NULL, evidence_type VARCHAR(32) NOT NULL, status VARCHAR(32) NOT NULL DEFAULT 'active', file_id UUID REFERENCES files(id) ON DELETE RESTRICT, summary_hash VARCHAR(64), latitude DOUBLE PRECISION, longitude DOUBLE PRECISION, accuracy_meters DOUBLE PRECISION, capture_status VARCHAR(32) NOT NULL, failure_reason VARCHAR(500), address_text VARCHAR(500), captured_by VARCHAR(64) NOT NULL, captured_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`); await q.query(`CREATE INDEX idx_evidence_records_business_status ON evidence_records (business_type,business_id,status)`); await q.query(`CREATE TABLE export_jobs (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), source_type VARCHAR(64) NOT NULL, source_id VARCHAR(128) NOT NULL, query_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb, export_format VARCHAR(16) NOT NULL, status VARCHAR(16) NOT NULL DEFAULT 'queued', result_file_id UUID REFERENCES files(id) ON DELETE RESTRICT, failure_message TEXT, retry_count INTEGER NOT NULL DEFAULT 0, requested_by VARCHAR(64) NOT NULL, requested_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), started_at TIMESTAMPTZ, finished_at TIMESTAMPTZ, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`); await q.query(`CREATE INDEX idx_export_jobs_status_requested ON export_jobs (status,requested_at)`); }
  async down(q: QueryRunner) { await q.query('DROP TABLE IF EXISTS export_jobs'); await q.query('DROP TABLE IF EXISTS evidence_records'); }
}
